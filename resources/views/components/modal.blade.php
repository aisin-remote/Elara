@props(['id', 'title'])
<dialog id="{{ $id }}" {{ $attributes->class(['w-[min(92vw,640px)] rounded-3xl bg-white p-0 shadow-2xl backdrop:bg-slate-950/60 dark:bg-slate-900']) }} aria-labelledby="{{ $id }}-title"><div class="flex items-center justify-between border-b border-slate-200 p-5 dark:border-slate-800"><h2 id="{{ $id }}-title" class="text-lg font-bold">{{ $title }}</h2><form method="dialog"><button class="grid size-10 place-items-center rounded-xl hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="Close {{ $title }}">✕</button></form></div><div class="p-5">{{ $slot }}</div></dialog>
